# Sticker images

Upload sticker images to this folder. Use stable numeric filenames such as `001.jpg`, `002.png`, or `003.gif`, then add each sticker's metadata to the root `stickers.json`.

The repository is public, so uploaded images can be viewed and downloaded by anyone.
